import { AppBar, Toolbar } from "@mui/material";

function Navbar() {
  return (
    <AppBar style={{ backgroundColor: "red" }}>
      <Toolbar>e</Toolbar>
    </AppBar>
  );
}

export default Navbar;
